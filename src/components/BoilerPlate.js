import React from 'react';

const BoilerPlate = (prop) => {
  return (
      <div className="portfolio__items">
          <h1>React Boilerplate</h1>
          <p>Base for React app with authentication</p>
      </div>
  )
};

export default BoilerPlate